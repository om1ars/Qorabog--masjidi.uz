import React from 'react'
import { Api } from '../../Api'

function Quran() {
    return (
        <div>
            {<Api.map((d) => {
                console.log();
            })}
        </div>
    )
}

export default Quran
