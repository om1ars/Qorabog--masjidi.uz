import React from 'react'
import { Api } from '../../Api'

function Quran() {
    return (
        <div>
            {<Api.map((d) => 
                console.log(d)
            
            })}
        </div>
    )
}

export default Quran
